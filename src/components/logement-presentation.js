function LogementPresentation(props) {
  const images = props.cover;
  const titles = props.title;
  const locations = props.location;
  const tags = props.tags;
  const ratings = props.rating;
  const names = props.host;
  const hostPictures = props.host;
  const descriptions = props.description;
  const equipements = props.equipments;
}

export default LogementPresentation;
